'use strict';
// import code
let React = require('react-native');
let {
  View,
  Text,
  StyleSheet,
} = React;

// create component
let Index = React.createClass({
  render: function () {
    return (
      <View>
        <Text>
          Index
        </Text>
      </View>
    );
  }
});

// create stylesheet

// module export
module.exports = Index;
